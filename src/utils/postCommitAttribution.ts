/**
 * Optional commit-attribution hook installer (ANT/internal). Stub for local fork.
 */
export async function installPrepareCommitMsgHook(
  _worktreePath: string,
  _worktreeHooksDir: string | undefined,
): Promise<void> {}
