# Claude API (bundled skill — stub)

Use the Anthropic API and official SDKs. This checkout ships **stub** markdown; replace `src/skills/bundled/claude-api/` with full docs for production.

Current model IDs (substituted at runtime): {{OPUS_NAME}} (`{{OPUS_ID}}`), {{SONNET_NAME}} (`{{SONNET_ID}}`), {{HAIKU_NAME}} (`{{HAIKU_ID}}`).

## Reading Guide

See `<doc path="...">` sections injected for your language below.

## When to Use WebFetch

If stub content is insufficient, fetch the latest official documentation.

## Common Pitfalls

- Do not rely on model IDs in this stub; verify against your gateway configuration.
