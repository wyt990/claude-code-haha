# Claude API documentation (stub)

This file is a **placeholder** for the local / leaked-source recovery build. Replace with full Anthropic Claude API / SDK documentation in a complete checkout.

- Model placeholders like `{{SONNET_ID}}` in SKILL.md are substituted at runtime from `SKILL_MODEL_VARS` in `claudeApiContent.ts`.

